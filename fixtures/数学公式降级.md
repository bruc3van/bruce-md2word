# 数学公式降级验收

普通导出应返回 `MATH_NOT_CONVERTED` 和源文件行号，文档保留公式的完整源码；严格模式应返回 `CONTENT_INCOMPLETE` 且不保存本次输出。

```sh
node lib/cli.js fixtures/数学公式降级.md -o output/数学公式降级.docx
node lib/cli.js fixtures/数学公式降级.md --strict -o output/数学公式降级-strict.docx
```

## 未知命令

$\unknown{x}$

## 参数缺失

$$\frac{a}$$

## 不支持的文档级标签与编号

$$\label{eq:test}x=1$$

$$x=1\tag{1}$$

## 不支持的自定义宏

$$\def\foo{x}\foo$$

## 不支持的颜色、隐藏内容和表格线

$$\color{red}x$$

$$\phantom{x}+y$$

$$\begin{array}{c|c}a&b\end{array}$$

## 不支持的结构

$$\cancel{x}$$

## 未闭合的显式行内公式

这里的公式未闭合：\(x+1

**降级样例文末标记：DSH-MATH-FALLBACK-END**
