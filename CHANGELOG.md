# v0.4.0

- 新增中文报告、技术文档排版预设，通过统一的 `word:document` 配置覆盖字体、字号、行距、首行缩进和页边距，CLI 与 DSH 共用。
- 新增可更新的 Word 目录、文档标题、页眉页脚和连续页码；支持独立标题多级编号、图表 SEQ 自动编号及 REF 交叉引用。
- 支持显式横向分节和纵向恢复，宽表使用分节实际宽度；正文从横向分节开始时，前置标题和目录保持纵向，避免多余空白页。
- 改进表格自动列宽与显式比例、跨页表头和拆行策略、标题及图表题注分页；正文缩进与列表、代码、图片的容器排版分开处理。
- 新增命名列表续号与按章节重启、Word 原生脚注和重复引用 NOTEREF 域；无效指令及不完整内容保留源文并报告诊断，严格模式拒绝降级输出。
- 补齐严格导出诊断在 CLI 与 DSH 间的传递，新增回归样例及可复现的 Word/WPS 验收脚本。真实 Word 16.0、WPS 12.0 已验证目录、页码、分节、图表引用、列表和脚注。
- 配置用法集中在 `docs/document-layout.md`，随 npm 包分发；默认调用保持兼容，继续沿用 HTML → DOCX 流程。

# v0.3.2

- Skill 优先复用已有可用版本，安装或更新遵循宿主审计、成熟期及构建授权策略，不默认追随 latest。
- 构建时移除 Mermaid 上游的 Google Fonts 外链，保留运行时外部资源检查和本地字体渲染。

# v0.3.1

- 将 `jszip` 从 `3.10.1` 升级到 `3.10.2`，包含二进制类型识别、Node.js Blob 支持和类型声明修复。

- 将 `markdown-it` 从 `14.1.1` 升级到 `14.3.2`，修复 smartquotes 拒绝服务告警 GHSA-6v5v-wf23-fmfq；继续关闭 typographer。

- CLI 新增 `--version` / `-v`，输出实际安装包版本；Skill 优先使用该入口检查版本，保留旧版查询方式。

- Skill 在首次使用时检查 CLI 的安装与版本，按需安装或更新后继续导出，并尊重指定版本、项目锁定版本和离线要求。

- GitHub 仓库统一为 `bruc3van/bruce-md2word`，同步仓库链接与 npm 发布配置；README 并列呈现Skill + CLI 和 DSH 插件入口。

# v0.3.0

- npm 包、CLI 命令和 DSH 插件标识统一为 `bruce-md2word`，不保留旧 CLI 别名；同步安装文档、示例、锁文件和发布工作流。GitHub 仓库地址保持不变。
- 新增中文 `bruce-md2word` Skill，随源码与 npm 包分发，包含严格导出、诊断修复和真实文件路径交付流程。

# v0.2.0

- 新增 Temml → MathML → OMML 数学公式转换，支持可编辑的 Word 公式、四种分隔符、矩阵及常见数学结构；不支持的公式保留完整源码并报告降级，严格模式拒绝保存。新增公式专项样例与回归测试。
- README 聚焦中文排版、Mermaid 图表和可编辑数学公式，新增 Microsoft Word 原生页面截图、可复现展示样例及 Agent 接口参考。
- 状态图遇到渲染器无法处理的语句时保留源码并报告降级，严格模式拒绝导出，避免注释静默丢失。
- 保留格式化文本之间的软换行分隔，包括紧凑列表；硬换行不引入多余空格。
- 普通图片与 Mermaid 共用最大显示高度，竖图按比例缩小。
- 诊断截断保留被合并提示的严重程度，纯信息提示不会误触发严格模式失败。
- 保留有序列表从 0 开始的编号。

# v0.1.4

- 按交付模式声明必需服务，依赖服务消失时撤销工具、取消任务并等待清理，恢复后重新注册。
- 为资源配置补充整数、范围约束和字段说明。
- Windows 使用 junction 执行越界防护测试，保留独立文件符号链接权限检查，并补充服务热卸载与恢复回归测试。

# v0.1.3

首次公开发布。提供独立 Markdown → Word CLI 和原生 DSH `word_export` 工具，默认将文件保存到会话项目的 `output/`，同名文件自动编号。

- 迁移 Bruce-doc-converter 的中文文档样式，支持标题、列表、表格、代码、引用及本地图片。
- 内置离线 Mermaid 渲染，覆盖流程图、状态图、时序图、类图、ER 图和 XY 图常用语法，无需 Chromium。
- 修正中文类成员宽度，支持流程图和状态图长标签换行，提示缩放后的小字号。
- 未渲染图保留源码并在文档中标注；严格模式拒绝降级导出，并返回明确错误。
- 提供资源限制、超时、取消及独立安装包测试。

运行环境为 Node.js 24，DSH 服务依赖支持 0.1.5-rc.2 和 0.1.6-alpha.2。Windows 实机和 Word 分页视觉效果仍待人工验证。
