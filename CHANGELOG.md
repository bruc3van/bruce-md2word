# v0.2.0

- 新增 Temml → MathML → OMML 数学公式转换，支持可编辑的 Word 公式、四种分隔符、矩阵及常见数学结构；不支持的公式保留完整源码并报告降级，严格模式拒绝保存。新增公式专项样例与回归测试。
- README 聚焦中文排版、Mermaid 图表和可编辑数学公式，新增 Microsoft Word 原生页面截图、可复现展示样例及 Agent 接口参考。
- 状态图遇到渲染器无法处理的语句时保留源码并报告降级，严格模式拒绝导出，避免注释静默丢失。
- 保留格式化文本之间的软换行分隔，包括紧凑列表；硬换行不引入多余空格。
- 普通图片与 Mermaid 共用最大显示高度，竖图按比例缩小。
- 诊断截断保留被合并提示的严重程度，纯信息提示不会误触发严格模式失败。
- 保留有序列表从 0 开始的编号。

# v0.1.4

- 按交付模式声明必需服务，依赖服务消失时撤销工具、取消任务并等待清理，恢复后重新注册。
- 为资源配置补充整数、范围约束和字段说明。
- Windows 使用 junction 执行越界防护测试，保留独立文件符号链接权限检查，并补充服务热卸载与恢复回归测试。

# v0.1.3

首次公开发布。提供独立 Markdown → Word CLI 和原生 DSH `word_export` 工具，默认将文件保存到会话项目的 `output/`，同名文件自动编号。

- 迁移 Bruce-doc-converter 的中文文档样式，支持标题、列表、表格、代码、引用及本地图片。
- 内置离线 Mermaid 渲染，覆盖流程图、状态图、时序图、类图、ER 图和 XY 图常用语法，无需 Chromium。
- 修正中文类成员宽度，支持流程图和状态图长标签换行，提示缩放后的小字号。
- 未渲染图保留源码并在文档中标注；严格模式拒绝降级导出，并返回明确错误。
- 提供资源限制、超时、取消及独立安装包测试。

运行环境为 Node.js 24，DSH 服务依赖支持 0.1.5-rc.2 和 0.1.6-alpha.2。Windows 实机和 Word 分页视觉效果仍待人工验证。
